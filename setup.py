from distutils.core import setup
from setuptools import find_packages


setup(
    py_modules=[],
    name="flaskner",
    version="0.1.0",
    description="A simple NER (named entity recognition) API using Flask and SpaCy",
)
